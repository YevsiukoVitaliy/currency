// material-ui
import { Paper } from '@mui/material';

// ==============================|| DASHBOARD - DEFAULT ||============================== //

const Partition = () => {
  //   const [transactions, setTransactions] = useState([]);

  //   // useEffect(() => {
  //   //   axios.get('').then((response) => {
  //   //     setTransactions(response.data);
  //   //   });
  //   // }, []);

  return <Paper>ggfddfs</Paper>;
};

export default Partition;
